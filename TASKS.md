# TASKS — SpécimenManager

> Backlog de mise à jour issu de la réunion du 10/07/2026.
> Utiliser ce fichier comme référence en début de chaque session Claude Code : indiquer quelle case cocher/traiter, cocher au fur et à mesure, et laisser une note si une tâche est bloquée.

---

## 🔸 À CLARIFIER — bloquant tant que non tranché

- [x] **Projet > Post Analytic (PA)** — préciser le périmètre exact *(21/07/2026 : c'est une référence budgétaire — champ "Poste analytique" ajouté)*
- [x] **Méthode > HLC** — n'existe pas encore, définir ses champs avant conception *(22/07/2026 : champs définis — collecteur(s) + plage horaire (heureDebut/heureFin, déjà existants), + comptage par tranche horaire à modéliser. Pas encore implémenté — cf. 2.5.)*
- [x] **Méthode > Type d'environnement** — définir la liste de valeurs possibles *(22/07/2026 : liste déjà seedée conservée telle quelle — Urbain, Péri-urbain, Rural, Forêt, Savane, Mangrove, Côtier.)*

---

## 1. Projet

- [x] Update (préciser le périmètre)
- [x] Delete (préciser le périmètre)
- [x] Post Analytic (PA) — *dépend de la clarification ci-dessus*

## 2. Mission

### 2.1 Statut
- [x] Supprimer le champ/la fonctionnalité "Statut"

### 2.2 Agent de terrain
- [x] Sélection multiple d'agents (max 20 agents)

### 2.3 Localité — Champs
- [x] Unifier "nom de la localité" / "toponyme"
- [x] Verrouiller (lecture seule) les champs Région, District, Commune, Fokontany, Lat, Long, Alt quand ils viennent d'un clic sur la carte
- [x] Gestion de plusieurs contacts locaux (ajout / modification / suppression)

### 2.4 Localité — Carte
- [x] Afficher les localités des missions déjà passées sur la carte
- [x] Clic sur une localité existante → auto-remplissage des champs + contacts
- [x] Si localité existante cliquée → permettre modif/suppression/ajout de contact directement

### 2.5 Méthode
- [x] Gestion de plusieurs méthodes de collecte par mission (liste extensible)
- [ ] HLC — *dépend de la clarification ci-dessus*
- [x] Flux "sélection méthode + nombre de pièges" → génération dynamique de N cartes piège (ex: CDC_1, CDC_2, CDC_3) avec : *(25/07/2026 : implémenté, cf. journal)*
  - [x] Type habitat
  - [x] Type d'environnement — *dépend de la clarification ci-dessus*
  - [x] Intérieur / Extérieur *(25/07/2026 : nouveau champ `interieurExterieur`, optionnel)*
  - [x] Date (préciser : pose ou relève ?) *(23/07/2026 : implémenté — `datePose`/`dateReleve`, cf. journal)*
  - [x] Notes / observations
  - [x] Localisation du piège (carte centrée sur la localisation mission)

### 2.6 Intégration Mission ↔ Méthode
- [x] Fusionner l'interface Méthode dans l'interface Mission

## 3. Dictionnaire (dico_taxo)
- [ ] Optimiser les informations de toutes les espèces — *(26/07/2026 : `nomCommun` et `description` vides sur les 5929 entrées, `auteur`/`annee` manquants sur ~11-12% — pas encore traité, cf. journal)*
- [x] Gérer les espèces non identifiées/non précisées (ex: Anopheles sp, Anopheles gambiae s.l.) *(26/07/2026 : "sp." ajouté partout où nécessaire, cf. journal — "s.l." (sensu lato) pas encore traité)*

---

## Ordre de traitement recommandé
1. Clarifications (section 🔸)
2. Localité — verrouillage des champs carte
3. Localité — carte des missions passées + auto-remplissage
4. Agent de terrain — sélection multiple
5. Méthode — gestion multiple + génération dynamique des pièges
6. Mission ↔ Méthode — intégration
7. Dictionnaire — optimisation + espèces non identifiées
8. Projet — Update / Delete / Post Analytic
9. Statut — suppression

---

## Journal de session
*(à compléter au fil des sessions Claude Code : date, tâches traitées, blocages rencontrés)*

- 10/07/2026 — Notes de réunion initiales collectées, backlog créé.
- 21/07/2026 — Section 2.3 (Localité — Champs) traitée en entier : nom/toponyme unifiés (mirroir en base), verrouillage géo avec bouton de correction manuelle, contacts multiples (nouvelle table `LocaliteContact` + migration), composant partagé `LocaliteFieldsForm` pour `NouvelleMission.jsx`/`MissionDetail.jsx`. Testé de bout en bout (API + navigateur). Bug latent corrigé au passage : `dateFin` vide faisait échouer toute création de mission.
- 21/07/2026 — Section 2.4 (Localité — Carte) traitée : la mini-carte du formulaire localité superpose désormais toutes les localités existantes (points gris, `GET /localites/carte` étendu avec contacts/lat/long/altitude), clic dessus → pré-remplissage des champs + contacts (copie indépendante, pas d'édition en place), reclic sur un nouveau point de carte réinitialise proprement le verrouillage géo. Testé en navigateur sur les deux points d'entrée (création + édition de mission).
- 21/07/2026 — Section 1 (Projet) traitée : Update et Delete étaient déjà prêts côté backend (PUT/DELETE existants, non exposés en UI) — ajout de l'édition en ligne + suppression avec confirmation dans `ProjetDetail.jsx` (pattern repris de `MoustiqueDetail.jsx`), gating aligné sur les rôles backend (chercheur+ pour modifier, admin pour supprimer). Post Analytic (PA) clarifié = référence budgétaire ("poste analytique") — nouveau champ `posteAnalytique` (migration + Zod + formulaires création/édition). Testé en navigateur (créer / modifier / supprimer).
- 22/07/2026 — 2.1 : suppression complète du statut de Mission (enum `StatutMission` + colonne DB supprimés par migration, Zod, service, audit, et tout l'affichage frontend — MissionsPage, MissionDetail, NouvelleMission, DashboardPage, GlobalSearch). La carte "Bilan missions" de `ProjetDetail.jsx` (répartition par statut) a été retirée entièrement ; "Couverture terrain" gardée avec juste le total de missions. 2.2 : plafond agents de terrain relevé de 5 à 20 (au passage, un second plafond de 5 codé en dur côté `missions.service.js` — indépendant du schéma Zod déjà à jour — a aussi été trouvé et corrigé). Testé en navigateur.
- 22/07/2026 — Clarifications Méthode tranchées : Type d'environnement garde la liste seedée existante ; HLC = collecteur(s)+plage horaire+comptage par tranche horaire (pas encore implémenté) ; carte piège = datePose+dateReleve (pas encore implémenté). 2.6 traité en premier comme prévu : gestion des méthodes fusionnée dans `MissionDetail.jsx` (liste par localité + modale d'ajout/édition, même pattern que Localités/Contacts), page globale `/methodes` + `/methodes/nouvelle` + entrée de nav supprimées (Méthode n'a jamais eu besoin d'exister hors du contexte Mission, comme Localité). Bug latent trouvé et corrigé au passage : `dateCollecte` vide faisait échouer la création/édition de méthode (même classe de bug que `dateFin` sur les missions). Reste à faire pour 2.5 : flux de génération dynamique de N pièges, champs Intérieur/Extérieur, datePose/dateReleve, et le modèle de données HLC (comptage horaire).
- 23/07/2026 — Suite à une question utilisateur ("comment ajouter une méthode à une nouvelle mission ?"), ajout de la gestion des méthodes en brouillon dans `NouvelleMission.jsx` : chaque localité du formulaire de création a maintenant sa propre section "Méthodes de collecte" (ajout/édition/suppression en mémoire, aucun appel API tant que la mission n'existe pas). À la soumission, les méthodes brouillon sont créées juste après leur localité respective, une fois l'id réel connu. Extraction de `MethodeFieldsForm` (composant partagé) depuis `MethodeModal` de `MissionDetail.jsx` pour éviter la duplication des champs. Vérifié : le flux UI de brouillon (ouverture modale, sélection, ajout à la liste, aucun appel réseau prématuré) confirmé en navigateur ; la séquence finale de soumission (mission → localités → méthodes avec id réel) vérifiée en rejouant exactement les mêmes appels API via curl — le clic final "Créer la mission" en conditions réelles navigateur n'a pas pu être re-testé cette session (l'outil de contrôle du navigateur est devenu instable en fin de session, décalage d'échelle capture/viewport puis timeouts). À revalider manuellement ou lors d'une prochaine session.
- 23/07/2026 — Retour utilisateur sur capture d'écran : bug de superposition corrigé (la carte de fond de `NouvelleMission.jsx` passait au-dessus de la modale "Nouvelle méthode" — `z-index:800` des contrôles internes de `MapPicker.jsx` non contenu, faute d'isolation de pile ; fix d'une ligne : `isolation: isolate` sur le conteneur de la carte, corrige le problème partout où il peut survenir). Trois optimisations ajoutées à `MethodeFieldsForm.jsx` : (1) carte "Localisation du piège" repliable par défaut (visible seulement si une position spécifique existe déjà, ou sur demande) ; (2) numéro de piège auto-incrémenté au choix du type (le prochain numéro libre pour ce type parmi les méthodes de la même localité, au lieu de rester bloqué sur "1") ; (3) superposition des pièges déjà existants sur la carte (comme pour les localités), avec un code couleur par type de méthode (palette fixe, hash stable sur le code) et une légende compacte n'affichant que les types présents à proximité — cliquer sur un piège existant préremplit type/habitat/environnement/position. Backend : `methodes.service.js` renvoie maintenant aussi lat/long de la localité parente (fallback quand le piège n'a pas sa propre position). Changements livrés mais pas encore testés en navigateur cette session — l'utilisateur a repris la main pour tester manuellement.
- 23/07/2026 — Retour utilisateur sur une 2e capture : les champs "Début"/"Fin" du formulaire méthode n'étaient que des heures sans date, alors qu'il faut la date+heure de pose du piège et de relève. Remplacement complet de `dateCollecte` (date seule) + `heureDebut`/`heureFin` (heure seule, string) par `datePose`/`dateReleve` (un seul champ date+heure chacun, tous deux optionnels) sur `MethodeCollecte` : migration `20260723000000_methode_pose_releve` (colonnes ajoutées, reprise best-effort des données existantes — `dateCollecte` → `datePose` sur les 11 méthodes qui en avaient une, `heureDebut`/`heureFin` non repris faute de date associée exploitable — puis anciennes colonnes supprimées), Zod (`methodes.schema.js` — regex `datetime-local`), service + contrôleur (audit), `MethodeFieldsForm.jsx` (deux `<input type="datetime-local">` remplaçant la grille à 3 colonnes), et mise à jour de tous les points d'usage (`MethodeModal` dans `MissionDetail.jsx`, `MethodeDraftModal` dans `NouvelleMission.jsx`, affichage carte `carte.controller.js`/`CartePage.jsx`, sélecteur `MethodeCascade.jsx`). Migration appliquée et `prisma generate` exécuté (backend arrêté au moment du run) ; vérifié directement en base qu'une méthode existante a bien récupéré sa date dans `datePose`. Lint + `node --check` OK sur tous les fichiers modifiés — pas encore testé en navigateur, l'utilisateur ayant la main sur le démarrage des serveurs.
- 23/07/2026 — Bug signalé par l'utilisateur (capture d'écran d'une erreur serveur `prisma:error` sur `dashboard.controller.js:50` : `Invalid prisma.mission.findMany()`). Régression oubliée lors de la suppression du statut de Mission (session du 22/07) : deux `select: { statut: true }` sur le modèle `Mission` avaient survécu à la migration qui a supprimé la colonne — `dashboard.controller.js` (missions récentes du dashboard) et `localites.service.js` (`INCLUDE_LIST.mission`, utilisé sur les listes de localités). Ni l'un ni l'autre n'était consommé côté frontend (`statut` n'apparaissait dans aucun composant), donc suppression pure du champ des deux `select`. Vérifié directement contre la base : les deux requêtes Prisma concernées s'exécutent maintenant sans erreur.
- 25/07/2026 — Reste de la section 2.5 traité : champ **Intérieur/Extérieur** (nouveau `interieurExterieur` optionnel à 2 valeurs sur `MethodeCollecte`, migration `20260725000000_methode_interieur_exterieur`, Zod/service/contrôleur/audit, 3e colonne dans `MethodeFieldsForm.jsx` à côté de Type d'habitat/Type d'environnement, prérempli aussi au clic sur un piège existant de la carte) et flux **"sélection méthode + nombre de pièges"** → génération en série. Clarifié avec l'utilisateur avant codage (comme demandé) : (1) nouveau bouton "Générer en série" à côté du "+ Ajouter" existant, dans `NouvelleMission.jsx` ET `MissionDetail.jsx` ; (2) les N cartes générées (CDC_1, CDC_2, CDC_3…) sont vides à part type/numéro — remplies une par une ensuite via le même flux d'édition qu'aujourd'hui, sur le modèle des cartes localité ; (3) chaque piège garde sa propre position, aucune position partagée à la génération. Implémenté via un nouveau composant modal `MethodeBulkModal` par contexte : version brouillon dans `NouvelleMission.jsx` (pousse N objets en mémoire dans `localite.methodes`, aucun appel API) et version persistée dans `MissionDetail.jsx` (boucle de N `POST /methodes`, puis `refresh()`) — les deux réutilisent la même logique de numérotation auto-incrémentée déjà en place pour l'ajout unitaire, et affichent un aperçu des identifiants (ex: "CDC_2, CDC_3, CDC_4") avant validation. Lint + `node --check` OK ; migration appliquée et `prisma generate` exécuté (l'utilisateur a dû arrêter son backend manuellement le temps du run, EPERM sinon) ; vérifié directement en base que le champ est bien lisible. Pas testé en navigateur cette session.
- 25/07/2026 — Retour utilisateur sur capture d'écran : la liste des méthodes générées en série (BG_1, BG_2, CDC_1…) avait une grande zone vide entre l'identifiant et les boutons d'action. Demande : y afficher les coordonnées GPS + l'altitude du piège. Clarifié avant codage : altitude = nouveau champ `altitudeM` sur `MethodeCollecte`, calculé automatiquement via l'API d'élévation (open-meteo, même mécanisme que pour les localités) dès qu'une position propre est posée sur la carte du piège ; affichage dans la liste dès qu'une position existe, propre au piège ou héritée de la localité (avec distinction visuelle — icône atténuée quand héritée). Migration `20260725010000_methode_altitude`, Zod/service/contrôleur/audit mis à jour. `MethodeFieldsForm.jsx` : lookup d'altitude sur changement de position (nouveau `handleMapChange`), affichage lat/long + champ altitude éditable sous la carte quand une position propre est précisée, sinon rappel discret des coordonnées héritées de la localité ; l'altitude/position sont aussi copiées au clic sur un piège existant. `MissionDetail.jsx` : badge GPS + altitude ajouté sur chaque ligne de la liste des méthodes (icône `MapPin`, position propre ou héritée de `l.latitude`/`l.longitude`/`l.altitudeM`). Migration appliquée, `prisma generate` exécuté, vérifié en base ; lint + `node --check` OK. Pas testé en navigateur cette session.
- 25/07/2026 — Utilisateur signale que la zone reste vide après le fix précédent. Cause : la capture testée venait en fait de la liste **brouillon** de `NouvelleMission.jsx` ("Méthodes de collecte", création de mission), pas de la liste persistée de `MissionDetail.jsx` ("Méthodes") — seule cette dernière avait reçu l'affichage habitat/date/GPS la fois précédente ; la liste brouillon n'avait jamais été mise à jour (elle ne rendait que l'identifiant + crayon/poubelle). Corrigé : même rendu de ligne ajouté à la liste brouillon (résolution des noms Type d'habitat/Type d'environnement via deux nouveaux fetchs `typesHabitat`/`typesEnv`, badge GPS+altitude avec fallback sur la localité brouillon). Lint OK. Point resté ouvert : si la localité elle-même n'a pas encore de position (carte jamais cliquée), le badge n'a rien à afficher — attendu, pas un bug.
- 25/07/2026 — Optimisation carte demandée : suppression d'Esri, amélioration de la barre de recherche. Clarifié avant codage : (1) Esri retiré complètement, il ne reste que Google Satellite (déjà en place comme fallback) — plus de sélecteur de couches puisqu'il n'y a plus qu'une seule option (`lib/mapLayers.js` simplifié à une seule entrée) ; (2) recherche améliorée sur les deux cartes concernées : `MapPicker` (sélection de position, formulaires Localité/Méthode) ET la page "Carte des collectes" qui n'avait encore aucune barre de recherche ; (3) améliorations : résultats Nominatim biaisés sur Madagascar (`viewbox`+`bounded=0`, non restrictif), navigation clavier (flèches + Entrée + Échap), fond de la barre passé de blanc quasi-opaque (`bg-white/55`) à quasi-transparent (`bg-white/10` + blur). Extraction d'un composant partagé `MapSearchBar.jsx` (recherche Nominatim + dropdown, ex-code dupliqué dans `MapPicker.jsx`) réutilisé tel quel dans `CartePage.jsx` (nouvelle barre en haut-à-droite, à l'emplacement de l'ancien sélecteur de couches — ne place pas de marqueur, fait juste `setView` sur le résultat choisi). `MapPicker.jsx` nettoyé (état/logique de bascule de couche retirés, dropdown fermé désormais via `onBlur` avec délai plutôt que par le clic sur la carte). Lint OK (2 warnings `react-hooks/exhaustive-deps` préexistants, non liés à ce changement). Pas testé en navigateur cette session.
- 25/07/2026 — Retour utilisateur sur capture d'écran : le menu déroulant "Type de méthode" affichait "CODE — Nom" (ex: "CDC — CDC_LIGHT_TRAP"), jugé pas assez lisible. Retiré le préfixe code sur les 3 listes "Type de méthode" du parcours Méthode (`MethodeFieldsForm.jsx`, et les deux modales "Générer en série" de `NouvelleMission.jsx`/`MissionDetail.jsx`) — n'affiche plus que le nom. Régression évitée au passage : la recherche interne du composant `Select` (`components/ui/Select.jsx`) ne filtrait que sur le label affiché, donc taper un code connu ("CDC") dans la recherche n'aurait plus rien trouvé une fois le code retiré du label — ajout d'un champ optionnel `keywords` sur les options (non affiché, mais inclus dans le filtre de recherche), câblé avec le code sur les 3 listes concernées. Point noté mais non traité (sur demande explicite) : certains `nom` du dictionnaire Type de méthode sont juste le code retapé en majuscules (ex: "BIOGENTS_TRAP", "CDC_LIGHT_TRAP", "DOUBLE_NET") plutôt qu'un libellé français comme les autres ("Capture au repos intérieur") — à corriger via la page Dictionnaire quand l'utilisateur aura les bons libellés en tête, pas de terminologie entomologique devinée à sa place. Portée volontairement limitée à Type de méthode — la même pattern "CODE — Nom" existe aussi sur Type de spécimen (`NouvelAutreSpecimen.jsx`), non touchée. Lint OK. Pas testé en navigateur cette session.
- 25/07/2026 — Retour utilisateur sur capture d'écran : section "Informations sur la mission" (`NouvelleMission.jsx`) visuellement déséquilibrée — grand espace vide à droite de "Date de fin". Cause identifiée : grille CSS `grid-cols-2` à 8 items en flux continu ; le bloc "Chef de mission" (item 3 — label + bascule + select/texte, donc plus haut qu'un champ simple) partageait sa ligne avec "Date de début", et "Date de fin" (item 5) atterrissait seule en début de ligne suivante car l'item 6 ("Agents de terrain", pleine largeur) ne pouvait pas tenir à côté d'elle. Suggéré 3 options avant codage (avec aperçus ASCII) ; retenu le changement minimal (1 ligne) : bloc "Chef de mission" passé en pleine largeur (`md:col-span-2`), sans réordonner aucun champ — conséquence automatique de la grille : Date de début/Date de fin s'apparient désormais naturellement sur la ligne suivante, même hauteur. Lint OK. Pas testé en navigateur cette session.
- 25/07/2026 — Demande utilisateur (capture d'écran d'un calendrier custom) : remplacer tous les calendriers natifs (`<input type="date">`/`type="datetime-local"`) par un design personnalisé. Clarifié avant codage : (1) composant maison sans dépendance (cohérent avec `Select.jsx` déjà existant) plutôt qu'une librairie (react-day-picker) ; (2) pour les 2 champs date+heure (pose/relève de piège), calendrier custom pour la date + deux menus déroulants bornés (Heure 00-23, Minute par pas de 5) pour l'heure plutôt qu'un sélecteur roue/spinner sur-mesure — répond aussi à la demande annexe (heure bornée 0-23h, pas de survol/scroll qui déborde, puisqu'un menu déroulant à liste fixe n'a pas ce problème par construction) ; (3) conversion complète, pas seulement les formulaires de création. Nouveau composant `components/ui/DatePicker.jsx` (portail comme `Select.jsx`, grille 6×7 avec jours du mois adjacent grisés/cliquables, surlignage du jour sélectionné en couleur primaire (pas le bleu de la capture, qui n'était qu'une référence de mise en page), repère "aujourd'hui", bouton "Aujourd'hui", locale française Lundi-Dimanche, parsing de date toujours par composants `{y,m,d}` — jamais `new Date(stringISO)` — pour éviter le décalage de fuseau horaire). `FormField.jsx` route désormais `type="date"` vers `DatePicker` et `type="datetime-local"` vers un nouveau composite `DateTimeField` (DatePicker + 2 Select Heure/Minute, même contrat `"YYYY-MM-DDTHH:mm"` qu'avant) — corrige donc automatiquement 8 champs déjà routés via FormField (missions, méthodes, projets, spécimens). Les 7 `<input type="date">`/`type="time"` bruts restants convertis un par un : 2 filtres de `RecherchePage.jsx`, 2 champs de `NouvelleManipulation.jsx` (labo — dont un `DateTimeField` local réécrit pour réutiliser `DatePicker`+`Select`), et les formulaires d'édition en ligne de `MoustiqueDetail.jsx`/`TiqueDetail.jsx`/`PuceDetail.jsx`/`AutreSpecimenDetail.jsx`. Plus aucun calendrier natif dans l'app. Lint OK sur tous les fichiers touchés, sauf 2 erreurs préexistantes et non liées repérées au passage dans des fichiers touchés (`NouvelleManipulation.jsx` : règle `react-refresh/only-export-components` sur l'export `MODULES` ; `AutreSpecimenDetail.jsx` : import `Link` inutilisé + `initEdit` référencée avant déclaration dans un `useEffect`) — signalées à l'utilisateur, non corrigées (hors périmètre de cette demande). Pas testé en navigateur cette session.
- 26/07/2026 — Section **Hôte** traitée suite à une question utilisateur ("on a oublié de donner l'ID de l'hôte ?"). Vérification confirmée en creusant le code : `Hote` n'avait aucun identifiant terrain (contrairement à Moustique/Tique/Puce/AutreSpecimen, tous dotés d'un `idTerrain` séquentiel par localité) — la liste affichait juste `#12`, et pire, le sélecteur "Hôte" des formulaires Tique/Puce n'affichait que le nom d'espèce, rendant deux hôtes de la même espèce indiscernables. Gap plus large trouvé en creusant : aucune page de détail/édition Hôte côté frontend (lignes de liste non cliquables, pas de route `/hotes/:id`) alors que le backend supportait déjà GET/PUT — une erreur de saisie devenait donc impossible à corriger dès qu'un tique/puce référençait l'hôte (suppression bloquée). Format d'identifiant clarifié avec l'utilisateur : `HOTE_<AAAAMM>_<n>` (ex `HOTE_202607_3`), AAAAMM basé sur la date de pose (`datePose`) de la méthode de collecte liée, compteur global par mois (pas par localité, contrairement aux spécimens — un hôte n'est pas un spécimen piégé). Implémenté : nouveau champ `Hote.idTerrain` (migration `20260726000000_hote_id_terrain`), génération dans `utils/idTerrain.js` (`generateHoteId`), nouvel endpoint `GET /methodes/:id/preview-hote-id` (miroir de `preview-id-terrain`), `hotes.schema.js`/`service.js`/`controller.js` mis à jour (champ optionnel côté client, comme les spécimens — modifiable si besoin). Composant `IdTerrainField.jsx` généralisé (props `buildPreviewUrl`/`label`/`formatHint`) pour être réutilisé tel quel sur `NouvelHote.jsx` sans dupliquer le composant. Nouvelle page `HoteDetail.jsx` (calquée sur `MoustiqueDetail.jsx` — vue + édition inline + suppression, gating par rôle correct `technicien`/`chercheur` cette fois, contrairement à la carte de rôles locale approximative déjà présente dans `HotesPage.jsx`/`MissionDetail.jsx`), route `/hotes/:id` ajoutée, lignes de `HotesPage.jsx` rendues cliquables + colonne ID mise à jour + recherche étendue à l'identifiant, `notifications.js` corrigé pour lier vers la fiche au lieu de la liste. Sélecteurs "Hôte" de `NouveauTique.jsx`/`NouveauPuce.jsx` mis à jour pour préfixer l'identifiant (disambiguation définitivement résolue, deux hôtes ne peuvent plus avoir le même libellé). Vérifié en base : génération testée contre une méthode réelle → `HOTE_202605_1`. Lint OK sur tous les fichiers touchés. Pas testé en navigateur cette session.
- 26/07/2026 — Section **Dictionnaire → Taxonomie des spécimens** attaquée ("l'arbre n'est pas cohérent"). Investigation en base avant tout codage (comme demandé) : la hiérarchie ordre→famille→sous_famille→genre→sous_genre→espece→sous_espece elle-même était 100% valide (0 violation contre les règles déjà en place dans `taxonomieSpecimens.controller.js`) — le vrai problème était **10 genres dupliqués en 2 lignes distinctes chacun**, dont les 4 genres de moustiques les plus critiques pour la surveillance (Anopheles, Aedes, Culex, Mansonia) + Nuttalliella (tique) + 5 genres de puces. Cause tracée précisément : la famille Culicidae a 48 genres rattachés directement, mais pour ces 4 genres précis quelqu'un a aussi renseigné la sous-famille correcte (Anophelinae/Culicinae) en créant un *nouveau* nœud genre au lieu de déplacer l'existant — ex. "Aedes" id6 (sous Culicinae, seulement 2 espèces : aegypti/albopictus) coexistant avec "Aedes" id49 (sous Culicidae directement, 83 enfants incluant toute la classification en sous-genres) — deux arbres déconnectés pour le même genre. Density check additionnel : 37 genres sur 241 (~15%) n'avaient aucune espèce nulle part en dessous, et comme les formulaires de saisie spécimen ne listent que le niveau "espece" (`niveau: 'espece'` en dur dans les requêtes), ces genres étaient purement invisibles/inutilisables dans l'appli malgré leur présence au dictionnaire — explique directement la suggestion "sp." de l'utilisateur. `nomCommun`/`description` vides sur 100% des 5905 entrées (hors scope de cette session, signalé). 3 décisions validées avec l'utilisateur avant codage : fusion automatique des 10 paires (garder la branche avec le plus d'enfants, rattacher l'autre dedans, repositionner sous la sous-famille si l'une des deux paires y était), ajout d'une espèce "sp." sous chaque genre encore sans espèce après fusion, garde-fou pour empêcher que ça se reproduise. Exécuté : script de fusion ponctuel (10/10 paires fusionnées sans perte — vérifié nœud par nœud, y compris une collision exacte "Idilla caelebs" correctement dédupliquée plutôt que déplacée) ; 35 espèces "sp." ajoutées aux genres encore vides après fusion (ex: "Borichinda sp.", affiché automatiquement correct par `taxoLabel()`/les options de formulaire existantes — aucun changement frontend nécessaire, la logique `${parent.nom} ${nom}` gère déjà ce cas). Garde-fou ajouté dans `taxonomieSpecimens.controller.js` : nouvelle fonction `checkDuplicate()` partagée par `create()`/`update()` — pour les niveaux ordre/famille/sous_famille/genre/sous_genre, l'unicité du nom est désormais vérifiée sur tout l'arbre (même `type`) et plus seulement sous le même parent (espece/sous_espece restent volontairement scopés au parent, un épithète spécifique pouvant légitimement se répéter entre genres) ; message d'erreur indique où le doublon existe déjà. Vérifié en base après coup : 0 doublon de genre restant, 0 violation de hiérarchie, total 5905→5929 nœuds (10 fusions −11 nœuds + 35 ajouts "sp." = 5929, cohérent). Aucune donnée de spécimen (Moustique/Tique/Puce/AutreSpecimen — tables vides) n'était rattachée aux nœuds fusionnés, donc opération sans risque. Reste ouvert pour une prochaine session : `nomCommun`/`description` à peupler, notation "s.l." (sensu lato, complexe d'espèces) pas traitée.
- 26/07/2026 — Retour utilisateur sur capture d'écran (2e passage sur la taxonomie) : dans l'arbre affiché, des genres (ex "Bironella") apparaissaient au même niveau visuel que les sous-familles au lieu d'être nichés dedans. Vérifié en base : 44 des 48 genres de la famille Culicidae étaient rattachés directement à la famille sans sous-famille (seuls Anopheles/Aedes/Culex/Mansonia — les 4 déjà corrigés dans l'entrée précédente — avaient une sous-famille). Confirmé et appliqué avec l'utilisateur : sous-famille Anophelinae = uniquement Anopheles/Bironella/Chagasia (classification standard, non controversée), tous les 42 autres genres de Culicidae → Culicinae. Rattachement en masse exécuté (parentId seul modifié, aucun enfant/espèce déplacé ni perdu) : Anophelinae passe de 1 à 3 genres, Culicinae de 3 à 45. Vérifié après coup : 0 genre orphelin restant sous Culicidae directement, 0 violation de hiérarchie, total de nœuds inchangé (5929). Piste annexe repérée en creusant (non traitée, à décider avec l'utilisateur) : "Nyssorhynchus" existe à la fois comme genre indépendant quasi-vide (juste son propre sous-genre nominotypique sans espèce) et comme sous-genre d'Anopheles avec 42 vraies espèces — même famille de problème que les 10 doublons fusionnés précédemment, mais rang différent (genre vs sous-genre) donc pas capté par le script de fusion initial ; probablement d'autres cas similaires à chercher si utile.
- 26/07/2026 — Retour utilisateur sur une 3e capture ("pourquoi il y a encore des espèces qui ont différent hiérarchie ?") : sous le genre Anopheles, `arabiensis`/`funestus`/`gambiae`/`mascarensis` apparaissaient directement sous le genre alors que d'autres espèces étaient nichées sous un sous-genre (ex Stethomyia). Cause identifiée : conséquence directe de la fusion des 10 doublons — le script avait volontairement rattaché les espèces de la branche "perdante" directement au genre "gagnant" sans deviner leur sous-genre, pour ne pas risquer d'erreur de classification. Vérifié que les 3 autres genres fusionnés (Aedes, Culex, Mansonia) avaient exactement le même symptôme. Corrigé avec confirmation de l'utilisateur, classification standard non controversée pour ces espèces vectorielles bien connues : Anopheles arabiensis/funestus/mascarensis/gambiae → sous-genre Cellia ; Aedes aegypti/albopictus → sous-genre Stegomyia ; Culex quinquefasciatus/antennatus → sous-genre Culex (nominotypique) ; Mansonia uniformis → sous-genre Mansonioides. Vérifié après coup : 0 espèce restant directement sous ces 4 genres, 0 violation de hiérarchie, total de nœuds inchangé (5929, pur déplacement de parentId).
- 26/07/2026 — Sur demande utilisateur ("cherchez les autres cas comme Nyssorhynchus"), recherche systématique sur tout l'arbre (tous types) des collisions de rang genre/sous-genre — un même nom existant à la fois comme genre indépendant et comme sous-genre d'un AUTRE genre (en excluant le cas normal du sous-genre nominotypique, qui porte le même nom que son propre genre parent). 10 cas trouvés : Ayurakitia, Borichinda, Bothaella, Hulecoeteomyia, Nyctomyia, Ochlerotatus (tous sous-genres réels d'Aedes dupliqués en genre quasi-vide) + Nyssorhynchus (déjà repéré, sous-genre d'Anopheles à 42 espèces vs genre indépendant à 1) + Carios (tique — ici le genre indépendant était la branche riche, 77 enfants, contre 1 seul sous le sous-genre chez Argas — sens inverse des autres cas) + Monopsyllus (puce — structure à 3 branches : genre + son propre sous-genre nominotypique + un sous-genre homonyme chez Ceratophyllus) + Paleoctenophthalmus (puce). Point soumis à l'utilisateur avant fusion : sous Nyctomyia, une espèce apparaissait sous deux orthographes quasi-identiques ("biunguiculatus" au sous-genre vs "biunguiculata" au genre — vraisemblablement le même épithète avec accord grammatical latin différent) ; l'utilisateur a choisi de garder les deux comme espèces distinctes plutôt que de présumer un doublon. Fusion exécutée avec la même règle que les 10 précédents (branche la plus riche gagne, y compris quand c'est le genre indépendant qui gagne comme pour Carios) ; gestion spéciale des 3 placeholders "sp." ajoutés en amont (Borichinda/Bothaella/Ochlerotatus) — supprimés plutôt que rattachés puisque la branche gagnante avait déjà de vraies espèces ; une collision exacte détectée et dédupliquée au passage (Monopsyllus "sciurorum", déjà présent directement sous le genre gagnant). Vérifié après coup : 0 collision de rang restante sur tout l'arbre, 0 violation de hiérarchie, total 5929→5914 nœuds (-15, cohérent avec 10 nœuds enveloppes supprimés + 3 placeholders "sp." obsolètes + 1 doublon exact).
- 26/07/2026 — Suite à "cherchez d'autres collisions similaires ailleurs dans l'arbre" : recherche élargie à toute paire de rangs structurels (pas seulement genre/sous-genre) sur tout l'arbre — 0 collision genuine restante en dehors du cas "Sousgenreincertain" (placeholder "sous-genre incertain" intentionnellement réutilisé sous 4 genres différents, normal, pas une erreur). Recherche complémentaire au niveau espece/sous_espece : 489 noms partagés trouvés au total, mais la quasi-totalité est un phénomène normal de la nomenclature binomiale (un épithète peut légitimement se répéter d'un genre à l'autre) — filtré pour ne garder que les **doublons exacts** (même nom, même parent précis) : **9 trouvés**, et il s'agit d'un bug introduit par ma propre correction précédente (les 9 espèces vectrices rattachées à Cellia/Stegomyia/Culex/Mansonioides existaient déjà dans ces sous-genres richement peuplés — mon script de rattachement ne vérifiait pas les collisions à ce moment-là, contrairement aux scripts de fusion suivants). Supprimé les 9 doublons introduits (gardé les versions préexistantes des branches riches, dont certaines avaient en plus la citation d'auteur zoologiquement plus correcte, ex "(Linnaeus)" avec parenthèses indiquant un genre de description différent). Recherche complémentaire ciblée (genre ayant à la fois espèces directes ET sous-genres, où une espèce directe porte le même nom qu'une espèce sous un de ses propres sous-genres) : 4 cas trouvés et corrigés — *Rhipicephalus microplus* (tique, la célèbre tique du bétail, alias *Boophilus microplus* — doublon vide supprimé), *Chiastopsylla mulleri* et *cophurus* (puces, sous-espèces déplacées vers la branche la plus riche avec gestion de collision), *Chiastopsylla octavii* (ici c'est la branche genre-directe qui était la plus riche, doublon sous-genre vide supprimé — confirme que la règle "le plus de contenu gagne" fonctionne dans les deux sens). Vérification finale complète sur toute la base : 0 doublon exact, 0 chevauchement genre/sous-genre, 0 collision structurelle genuine, 0 violation de hiérarchie. Total 5914→5901 nœuds (-13, cohérent). L'arbre taxonomique est maintenant considéré propre sur tous les axes vérifiés à ce jour.
- 26/07/2026 — Demande utilisateur : ajouter de l'explication/contexte sur la page "Taxonomie spécimens" (capture d'écran) — suggérer une approche avant de coder. Proposé 3 options (panneau repliable, icône d'aide + modale, ou les deux + compteurs) ; retenu le panneau repliable sous les filtres. Implémenté `HierarchyInfoPanel` dans `TaxonomieSpecimensPage.jsx` : fermé par défaut, état mémorisé par utilisateur (`localStorage`), contenu = les 7 niveaux (ordre→sous-espèce) avec une ligne d'explication chacun + 2 encadrés sur les conventions découvertes/appliquées cette semaine ("sp." = espèce non déterminée ; sous-genre/sous-espèce nominotypique = même nom que le parent, normal). Lint OK. Pas testé en navigateur cette session.
- 26/07/2026 — Retour sur Mission/Méthode : (1) "verrouiller le fait qu'une Mission avec une localité regroupe plusieurs méthodes" — clarifié avec l'utilisateur (ambigu au départ) : pas une contrainte de validation, mais rendre le regroupement Localité→Méthodes plus visible à l'écran. Ajouté sur `MissionDetail.jsx` ET `NouvelleMission.jsx` : badge "N méthode(s)" bien visible à côté du nom de chaque localité (pas seulement dans le petit sous-titre de la section), et la section "Méthodes de collecte" de chaque localité est maintenant encadrée (bordure + fond légèrement teinté) au lieu d'être juste séparée par une ligne — se voit clairement comme un sous-groupe de la localité plutôt qu'une liste posée à côté. Dans `NouvelleMission.jsx`, le compteur apparaît aussi dans les onglets de localité (ex "1. AKZ (2)") et le nom de la localité est ajouté à l'en-tête "Localité n°1 — AKZ". (2) Zoom de la carte "Localisation du piège" (`MapPicker.jsx`) resserré : passait de 6 (vue large) à 12 quand une position était connue (localité ou piège) — porté à 15 (zoom précis, niveau rue) dans les deux endroits concernés (vue initiale au montage + suivi des changements de coordonnées), pour repérer un piège plus précisément que ce que permettait le zoom régional précédent. Lint OK (2 warnings react-hooks/exhaustive-deps préexistants dans MapPicker.jsx, déjà notés en session précédente). Pas testé en navigateur cette session.
- 26/07/2026 — Retour utilisateur sur capture d'écran de la modale "Nouvelle méthode" : (1) l'en-tête bleu détonnait par rapport au reste de l'appli — vérifié : c'était `bg-info` (bleu) alors que toutes les autres modales (ex Localité) utilisent le dégradé `from-primary-600 to-primary-500` (vert de la marque) comme couleur de base. Corrigé sur les 4 en-têtes concernés — `MethodeModal` et `MethodeBulkModal` dans `MissionDetail.jsx`, `MethodeDraftModal` et `MethodeBulkModal` dans `NouvelleMission.jsx` — désormais alignés sur le dégradé primaire comme le reste de l'appli. (2) Barre de recherche indésirable dans les champs Heure/Minute (`Select` bascule automatiquement en mode recherche dès que la liste dépasse 8 options — 24 heures et 12 minutes en ont largement) — supprimée en passant `searchable={false}` sur les 4 `<Select>` Heure/Minute concernés (`DateTimeField` dans `FormField.jsx`, `DateTimeField` local de `NouvelleManipulation.jsx`). Lint OK (1 erreur préexistante non liée déjà signalée dans `NouvelleManipulation.jsx`). Pas testé en navigateur cette session.
- 26/07/2026 — Retour utilisateur sur capture d'écran de la barre de recherche carte : un rectangle blanc opaque cassait l'effet "verre dépoli" demandé précédemment. Root cause identifiée (pas un simple choix de design) : une règle CSS globale `input[type="text"] { background-color: ... }` dans `index.css` (ajoutée pour le dark mode des champs hors `FormField`) a une spécificité CSS supérieure à la classe utilitaire `bg-transparent` seule et l'emportait silencieusement, forçant un fond opaque malgré le code. Suggéré 2 directions avant de coder (corriger le verre dépoli vs. passer à une carte blanche opaque façon Google Maps) ; retenu : corriger le verre dépoli. Corrigé avec le modificateur `!important` de Tailwind (`!bg-transparent`, ciblé sur ce seul champ — la règle globale reste utile ailleurs, non touchée) + renforcé le style pour plus de robustesse visuelle : flou `backdrop-blur-md`→`lg`, ombre `shadow-md`→`lg`, léger liseré de texte blanc (`text-shadow`) pour garder le texte lisible même sur un fond de carte sombre (forêt, océan). Lint OK. Pas testé en navigateur cette session — à vérifier notamment sur des zones de carte très claires (sable, bâtiments) et très sombres.
- 27/07/2026 — Demande utilisateur : corriger les 42 problèmes lint dans `router/index.jsx`. Cause : ce fichier n'exporte qu'un seul objet (`router`, la config du routeur — pas un composant), donc `react-refresh/only-export-components` signale chacune des ~40 pages en `lazy()` comme "export non-composant" — react-refresh ne peut de toute façon pas faire de hot-reload fin sur ce fichier quelle que soit sa structure (éditer ce fichier recharge toujours la page entière), donc pas un vrai bug à corriger dans le code, juste un faux positif structurel déjà reconnu : les 4 composants de garde de route (`ProtectedRoute`, `AdminRoute`, `PublicRoute`, `MinRoleRoute`) avaient chacun leur propre `// eslint-disable-next-line` pour la même raison. Remplacé par un unique `/* eslint-disable react-refresh/only-export-components */` en tête de fichier (avec commentaire expliquant pourquoi) et supprimé les 4 désactivations ligne-par-ligne devenues redondantes (auraient sinon déclenché un nouvel avertissement "unused eslint-disable directive"). 42 erreurs → 0 problème. Aucun changement fonctionnel, uniquement du lint.
- 27/07/2026 — Suite au déploiement/vérification de la journée (403 public, Tailscale), demande utilisateur : une vraie interface pour "erreur de connexion". Investigation avant codage : le code avait déjà la notion — `authStore.js` avait un message de secours codé en dur `'Erreur de connexion'` (repris tel quel par l'utilisateur dans sa demande) pour le cas où une requête échoue sans réponse HTTP du tout, mais affiché avec le même style qu'une erreur de saisie classique (identifiants invalides) ; et l'intercepteur Axios global (`axios.js`) gérait déjà 401/429/500+ mais ne faisait strictement rien pour l'absence totale de réponse (panne réseau, backend injoignable, timeout) — chaque page se débrouillait alors avec un message générique "Erreur". Portée confirmée avec l'utilisateur avant codage : page de connexion + bandeau global. Implémenté : nouveau store `connectionStore.js` (état `isDown` global) ; `axios.js` met à jour ce store (`setUp()` sur toute requête réussie, `setDown()` quand `!error.response` — pas de toast pour ce cas précis, pour éviter un spam d'un message par requête en échec) ; nouveau `ConnectionBanner.jsx` monté dans `MainLayout.jsx` (bandeau fixe en haut, discret, avec ping `/api/health` toutes les 5s pendant qu'il est affiché pour se masquer automatiquement dès que le serveur redevient joignable, sans attendre une action utilisateur) ; `authStore.js` distingue désormais `isNetworkError` (déduit de `!err.response`) du message d'erreur classique ; `LoginPage.jsx` affiche un traitement dédié quand c'est le cas (icône wifi-barrée, ton "warning" plutôt que "danger", conseil VPN/Tailscale, bouton Réessayer) au lieu du texte rouge générique. Vérifié : `WifiOff`/`RotateCw` existent bien dans lucide-react. Lint OK sur tous les fichiers touchés. Pas testé en navigateur cette session — à essayer en coupant le backend localement pour valider le comportement des deux écrans.
- 28/07/2026 — Retour utilisateur : test en DevTools "Offline" sur une page pas encore chargée (`/missions`) a fait planter l'appli avec l'écran d'erreur générique de React Router ("Unexpected Application Error!"), pas le bandeau construit hier. Cause expliquée : le mode Offline des DevTools bloque toutes les requêtes réseau, pas seulement les appels API — en dev Vite, chaque page est un chunk JS chargé à la demande via `lazy()`, donc naviguer vers une page jamais visitée pendant qu'on est hors-ligne fait échouer le `import()` dynamique lui-même, un problème différent (chargement de page) de celui qu'on traite (perte de connexion à l'API). Le message de la console suggérait lui-même la solution ("provide your own ErrorBoundary or errorElement"). Corrigé : nouveau `RouteErrorBoundary.jsx`, ajouté en `errorElement` sur les 3 routes racines du routeur (`/login`, `/register`, `/`) dans `router/index.jsx` — distingue un échec de chargement de chunk (regex sur le message d'erreur, formulation différente selon les navigateurs : Chrome/Firefox/Safari) affiché comme "Nouvelle version disponible" (cas réel possible : redéploiement pendant qu'un onglet reste ouvert, anciens fichiers JS disparus) d'une erreur JS inattendue générique, dans les deux cas avec un bouton "Recharger la page". Repris le même habillage visuel que `LoginPage.jsx` pour la cohérence. Lint OK. Pas testé en navigateur cette session.
- 29/07/2026 — Demande utilisateur : récap complet du système de rôles avant qu'il propose ses propres idées d'optimisation. Fourni un tableau par rôle (lecteur→admin) croisant routes backend (`requireRole`/`requireMinRole` sur tous les fichiers de `routes/`), cloisonnement projet (`access.js` — admin/superviseur bypass, les autres limités à `MembreProjet`) et `specimensAutorises`. Bug concret trouvé pendant la revue : 5 pages frontend (`HotesPage.jsx`, `ReferentielSimplePage.jsx`, `TaxonomieHotesPage.jsx`, `TaxonomieSpecimensPage.jsx`, `MissionDetail.jsx`) définissaient chacune une carte de rôles locale obsolète `{ admin:4, chercheur:3, terrain:2, lecteur:1 }` — sans `superviseur` du tout — utilisée pour `canEdit = isMin(user?.role, 'chercheur')` ; comme `ROLES['superviseur']` valait `undefined→0`, un superviseur (niveau 4, largement au-dessus de chercheur) se voyait refuser l'affichage des boutons modifier/créer sur ces 5 pages alors que le backend acceptait bien sa requête. `HoteDetail.jsx` avait déjà la bonne carte (`{admin:5, superviseur:4, chercheur:3, technicien:2, lecteur:1}`), servie de modèle. Corrigé sur les 5 fichiers, lint OK. Pas de changement backend.
- 29/07/2026 — Demande utilisateur : intégrer `frontend/public/icons/logo.png` (nouveau logo fourni par l'utilisateur — Madagascar stylisé avec moustique/tique/puce) comme logo de marque, en lieu et place de l'icône générique `FlaskConical` (lucide) utilisée jusqu'ici sur login/register/écran d'erreur/sidebar (les icônes `FlaskConical` décoratives des pages Labo, elles, n'ont pas été touchées — elles représentent du matériel de labo, pas la marque). Retour utilisateur (captures d'écran) : logo "très petit, invisible" une fois en place. Root cause investiguée avant tout ajustement CSS : le PNG source (1536×1024) avait d'énormes marges transparentes autour du dessin réel (bbox mesurée par script PowerShell/System.Drawing sur le canal alpha : x 439–904, y 104–850, soit ~30% de la largeur utile) — `object-contain` réduisait donc le dessin bien plus qu'il n'y paraissait. Recadré le PNG sur son contenu (+padding ~5%) via System.Drawing (`Bitmap.Clone` sur le rectangle mesuré), original conservé sous `logo-original.png`. Deuxième passe (vérifiée en navigateur via Claude-in-Chrome, captures zoom) : le badge login/register/erreur utilisait un fond `bg-white/15` translucide sur le dégradé vert foncé — contraste insuffisant pour le tracé noir du logo. Basculé sur fond blanc opaque + ombre (cohérent avec le traitement déjà utilisé dans la sidebar `MainLayout.jsx`, qui elle n'a pas eu ce problème). Puis, sur demande explicite de l'utilisateur ("augmenter encore", sans re-tester moi-même), badges login/register/erreur agrandis une fois de plus (64px→80px, logo 48px→64px) et sidebar (40px→48px desktop, 28px→36px mobile). Favicon (`index.html`) basculé sur le même PNG. Lint OK sur tous les fichiers touchés.
- 29/07/2026 — Demande utilisateur (optimisation de l'interface d'authentification) : (1) remplacer le sous-titre "Institut Pasteur de Madagascar" par "Unité Entomologie Médicale" sur `LoginPage.jsx`/`RegisterPage.jsx` (texte en dur, pas encore passé par `i18n.js` — `footer.subtitle`/`sidebar.subtitle` y existent déjà avec la variante "(UEM)", non réutilisés ici pour respecter le libellé exact demandé). (2) "Mot de passe oublié ?" — vérifié qu'aucune infra email (SMTP/nodemailer) n'existe dans le projet ; portée clarifiée avec l'utilisateur avant codage (lien simple "contactez un admin" vs. flux email complet) → retenu le lien simple, car un admin peut déjà réinitialiser un mot de passe via `PATCH /auth/users/:id/reset-password` (existant, `UtilisateursPage.jsx`). (3) Question de suivi de l'utilisateur ("et si l'utilisateur veut changer son mot de passe lui-même ?") a révélé un vrai manque : aucun endpoint self-service pour qu'un utilisateur déjà connecté change son propre mot de passe. Confirmé avec l'utilisateur et implémenté les deux : nouvel endpoint `PATCH /api/v1/auth/me/password` (vérifie l'ancien mot de passe via bcrypt avant de le remplacer, schéma Zod `changePassword`), nouveau `ChangePasswordModal.jsx` (calqué sur le `ResetPasswordModal` admin existant dans `UtilisateursPage.jsx`) accessible via une icône clé dans le profil de la sidebar (`MainLayout.jsx`) ; lien "Mot de passe oublié ?" ajouté sur `LoginPage.jsx` au-dessus du champ mot de passe, affichant au clic un encart expliquant de contacter un administrateur. Lint (`eslint`) + vérification de syntaxe (`node -c`) OK sur tous les fichiers touchés. Pas testé en navigateur cette session pour ce dernier point (changement/reset mot de passe).

- 29/07/2026 — **Audit global + Livre de Conception PDF.** À la demande de finaliser l'application, produit un audit technique complet (bugs probables + améliorations, front & back) sous forme d'un **livre de conception** détaillé en PDF : `docs/SpecimenManager_Livre_de_Conception.pdf` (16 pages ; source régénérable `docs/*.source.html`). Pipeline de génération : HTML autoportant + Mermaid v11 (téléchargé localement) rendu en PDF via **Edge headless** (`--headless=new --no-pdf-header-footer --virtual-time-budget`), tous les diagrammes rendus en vectoriel. Contenu : présentation/glossaire, architecture en couches, **2 diagrammes de classes UML** (domaine terrain + module Labo), hiérarchie de containment, **cas d'usage + matrice RBAC**, **3 diagrammes de séquence** (auth, saisie spécimen, workflow labo), **2 diagrammes d'états** (ManipulationLabo, Projet), référentiels/énumérations, chapitre audit (7 anomalies backend + 3 frontend avec correctifs), roadmap 3 sprints. Audit basé sur lecture ciblée : schéma Prisma, `auth.middleware.js`, `auth.controller.js`, `labo.controller.js`, `notifications.controller.js`, `access.js`, `upload.middleware.js`, `axios.js`, `authStore.js`.

- 29/07/2026 — **Sprint 1 de finalisation (correctifs bloquants de l'audit).** (B1) `labo.controller.js › deleteManipulation` : la regex `/^labo\/[^/]+\//` sur-découpait le chemin (UPLOADS_ROOT finit déjà par `/labo`) → les fichiers gels/.fastq n'étaient jamais effacés du disque. Corrigé en `/^labo\//`. (B2) **Révocation de session** : `verifyToken`/`verifyTokenSSE` (auth.middleware.js) passés en async — revérifient désormais `actif`+`role` en base via un cache court 30 s (`loadUserStatus`/`enforceAccountActive`), fail-open sur indispo DB (repli sur le JWT signé). Un compte désactivé/supprimé → 401 sous ≤30 s ; le rôle est rafraîchi depuis la base (enforce aussi les changements de rôle). Nouvel export `invalidateUserStatus`, appelé dans `auth.controller.js` sur `updateUser` (si role), `activateUser`, `deleteUser`. (B5) **« Autres spécimens » attribuables** aux rôles non-admin : ajout de `'autre'` à `specimensEnum` (auth.schema.js), `SPECIMENS_VALIDES` (auth.controller.js) et à la liste `SPECIMENS` de `UtilisateursPage.jsx` (case à cocher). (Item 4) Seed admin : mot de passe lu depuis `SEED_ADMIN_PASSWORD` (fallback `Admin1234!` + warning). Validé : `node --check` sur les 5 fichiers backend, `eslint` sur le fichier front, et **smoke-test complet 28/28 OK** (serveur redémarré pour l'occasion — login admin passe bien par le nouveau verifyToken async). Restent Sprint 2 (B3, B4, F1, F3) et Sprint 3 (tests/CI, B6, B7, F2).

- 30/07/2026 — **Sprint 2 de finalisation (intégrité & expérience).** Les 4 correctifs de l'audit sont livrés. **B4** (`labo.controller.js › updateManipulation`) : un PUT partiel (ex. `{ notes }`) réécrivait à `null` toutes les données scientifiques du module — `buildModuleData` renvoie tous les champs du module, les absents devenant `null`. Corrigé : on filtre `modData` pour ne garder que les clés réellement présentes dans `req.body`, et on n'exécute l'`upsert` du module que s'il reste au moins un champ (`hasModData`). **F1** (rôle en temps réel) : backend émet déjà `sseManager.sendToUser(id, 'account_updated', { role, message })` dans `updateUser`/`updateUserStatus` (`auth.controller.js`) ; `NotificationBell.jsx` écoute l'événement `account_updated` et appelle `updateUser({ role })` + toast → barre latérale/boutons rafraîchis sans reconnexion. **F3** (redirection 401 en rafale) : flag module `isRedirecting401` dans `axios.js` — la 1re 401 nettoie le token + planifie la redirection, les suivantes sont ignorées (plus d'empilement de toasts/redirections). **B3** (état « lu » des notifications PAR utilisateur) — le gros morceau : `audit_logs.isRead` était un booléen partagé, donc marquer une notif lue la masquait pour *tout le monde* et faussait chaque `unreadCount`. Nouvelle table `NotificationRead` (`notification_reads`, unique `(user_id, audit_log_id)`, FK cascade sur users + audit_logs), migration `20260729000000_add_notification_read`. `notifications.controller.js` entièrement recâblé sur la relation : `list` calcule `isRead` par utilisateur (`reads: { where:{userId} }`) et `unreadCount` via `reads: { none:{userId} }` ; `markRead` fait un `upsert` idempotent ; `markAllRead` insère en masse (`createMany` + `skipDuplicates`) ; le filtre `?isRead=` et le `unreadCount` initial du flux SSE passent aussi par la relation. Contrat d'API inchangé côté frontend (`item.isRead` booléen + `unreadCount`) — aucun changement `NotificationBell.jsx`/`NotificationsPage.jsx` nécessaire pour la lecture. `audit_logs.isRead` conservé pour compat mais n'est plus la source de vérité. Validé : migration appliquée (`migrate deploy`) + `prisma generate` ; test dédié des vrais handlers (req/res simulés, 2 utilisateurs) **11/11 OK** — confirme l'isolation par utilisateur (A marque lu → B reste non lu), `unreadCount` par utilisateur, filtres et `markAllRead` ; **smoke-test complet 28/28 OK** (aucune régression). Reste Sprint 3 (tests/CI, B6, B7, F2). NB : Sprint 1 + Sprint 2 sont dans l'arbre de travail, **pas encore committés**.

- 30/07/2026 — **Sprint 3 de finalisation (fiabilisation & industrialisation).** Les 4 chantiers traités (choix utilisateur : périmètre complet + profondeur « fondation + tests clés »). **B7 — code mort & double validation** : `upload.middleware.js` réduit à la seule constante `UPLOADS_ROOT` (les middlewares `uploadGel`/`uploadFichier` n'étaient utilisés nulle part — `labo.routes.js` a son propre `makeUpload`) ; `auth.controller.js` allégé de toutes les validations manuelles redondantes + transforms/defaults déjà appliqués par Zod (vérifié : `validate()` remplace `req.body` par les données parsées, et les 8 routes auth touchées ont bien `validate(schema.*)`) — seule la logique métier est conservée (conflits email 409, anti-auto-désactivation, compare bcrypt) ; constantes `ROLES_VALIDES`/`SPECIMENS_VALIDES` supprimées (mortes). Périmètre volontairement limité aux cibles nommées par l'audit (pas de chasse dans les 25 contrôleurs). **B6 — intégrité des relations polymorphes** : `ManipulationLabo`/`PoolMembre` ciblent un spécimen via `(specimenType, specimenId)` sans FK → supprimer un spécimen laissait des lignes orphelines. Choix utilisateur = **bloquer (409)** plutôt que cascader (préserve les résultats labo). Nouveau helper partagé `utils/specimenRefs.js` (`countSpecimenRefs`/`refsReason`/`findReferencedSpecimenIds`) ; garde ajoutée aux 4 handlers `delete` unitaires (moustique/tique/puce/autre → 409 si référencé) et à `bulkDeleteMoustiques` (exclut les référencés du lot et rapporte `skipped` au lieu de bloquer tout). **F2 — centralisation RBAC** : nouvelle source de vérité unique `config/rbac.js` (`ROLE_ORDER`→`ROLE_LEVELS` dérivés, `BYPASS_ROLES`, `SPECIMEN_TYPES`, `DEFAULT_SPECIMEN_ACCESS`) ; `auth.middleware.js`, `auth.schema.js` (`z.enum(ROLE_ORDER)`/`z.enum(SPECIMEN_TYPES)`) et `utils/access.js` la consomment (fin de la triple duplication backend) ; nouvel endpoint `GET /api/v1/rbac/config` (auth requise) ; côté frontend `lib/roles.js` refactoré — hiérarchie **hydratée** au démarrage depuis l'endpoint (`hydrateRbac`, appelé dans `MainLayout`) avec repli statique + `console.warn` de dérive en dev. **Tests + CI** : Vitest+Supertest ajoutés au backend (`tests/unit/` DB-free : `rbac.test.js`, `authSchema.test.js` → **16 tests**), Vitest+React Testing Library au frontend (`roles.test.js`, `Badge.test.jsx` → **8 tests**, config jsdom + setup jest-dom) ; scripts `test`/`test:watch`/`test:smoke` (le stub « Tests à venir » remplacé). Workflow **`.github/workflows/ci.yml`** (2 jobs) : `frontend` (npm ci → lint → test → build) et `backend` (service Postgres/PostGIS → npm ci → prisma generate → vitest → migrate deploy → seed → smoke-test 28 cas). Au passage, pour que `npm run lint` (bloquant en CI) passe : 9 erreurs lint **préexistantes** (hors de mes fichiers) traitées — vrais correctifs sûrs (`no-unused-vars` ×3 dans ImportPage/AutreSpecimenDetail, remontée de `initEdit` avant son `useEffect` pour `react-hooks/immutability`) + alignement des règles purement advisory de react-hooks v7 sur la politique déjà en place (`react-hooks/static-components` → `warn`, comme `set-state-in-effect: 'off'`) et disables fichier `react-refresh/only-export-components` sur AuthImg/NouvelleManipulation (précédent `router/index.jsx`). **Validation** : backend vitest **16/16**, frontend vitest **8/8**, lint frontend **0 erreur** (8 warnings advisory), build frontend OK, **smoke-test 28/28**, et vérif ciblée dédiée **13/13** (endpoint `/rbac/config` : 401 sans token, `superviseur=4`/`admin=5`, `autre` présent, bypass=admin+superviseur ; garde B6 : `countSpecimenRefs`/`refsReason`/`findReferencedSpecimenIds` corrects, total→0 après détachement). Les 3 sprints d'audit (v1.0) sont désormais terminés. NB : Sprint 1 + 2 + 3 sont dans l'arbre de travail, **toujours pas committés** — à committer (proposé : 3 commits séparés par sprint).
